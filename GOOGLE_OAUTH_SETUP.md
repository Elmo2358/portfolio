# Google Cloud Console 設定手順

## 1. プロジェクト作成後の確認

プロジェクトを作成したら、上部のプロジェクト選択ドロップダウンで作成したプロジェクトが選択されていることを確認してください。

## 2. Google Tasks APIを有効化

1. 左側のメニューから「APIとサービス」→「ライブラリ」をクリック
2. 検索バーに「Tasks API」と入力
3. 「Google Tasks API」をクリック
4. 「有効にする」ボタンをクリック

## 3. OAuth同意画面の設定

1. 左側のメニューから「APIとサービス」→「OAuth同意画面」をクリック
2. 「外部」を選択して「作成」をクリック

### 必須フィールド（最低限入力するもの）:
- **アプリ名**: Portfolio Hub
- **ユーザーサポートメール**: 自分のメールアドレス
- **デベロッパーの連絡先情報**: 自分のメールアドレス

3. 「保存して次へ」をクリック（スコープは後で設定）
4. 「保存して次へ」をクリック（テストユーザーは空でOK）
5. 「保存して次へ」をクリック（概要を確認）

## 4. OAuth 2.0クライアントIDの作成

1. 左側のメニューから「APIとサービス」→「認証情報」をクリック
2. 上部の「+認証情報を作成」をクリック
3. 「OAuthクライアントID」をクリック

### アプリケーションの種類: 「ウェブアプリケーション」を選択

### 認証情報を作成:

**名前**: Portfolio Hub（任意）

**承認済みリダイレクトURI**（重要！）:
```
http://localhost:3000/api/auth/callback/google
```

4. 「作成」をクリック

## 5. クライアントIDとシークレットの取得

作成完了後、クライアントIDとクライアントシークレットが表示されます。

これを`.env.local`ファイルに設定してください：
```
GOOGLE_CLIENT_ID="取得したクライアントID"
GOOGLE_CLIENT_SECRET="取得したクライアントシークレット"
```
